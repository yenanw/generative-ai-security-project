# This .gitignore is a whitelist, because we don't really want all the random
# compilation file and executables to be included
*

!.gitignore
!*/
!**/*.c
!**/*.md
!**/*.txt
!**/*.png
!**/*.jpg
!**/*.jpeg

# Allow all in the output folder of the fuzzer, assuming the folder is named
# [something]out/
!**/*out/**
